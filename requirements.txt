streamlit==1.63.0
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
plotly==7.0.0
mlxtend==0.25.0
scipy==1.17.1
statsmodels==0.15.0
pyarrow==25.0.1

# project 08 (nano transformer LLM) only — NOT installed by this file.
# Install separately from the small CPU-only wheel index:
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
